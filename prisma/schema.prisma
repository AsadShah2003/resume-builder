generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model resume {
  id          Int       @id @default(autoincrement())
  authorID    Int       @unique
  createdAt   DateTime  @default(now())
  updatedAt   DateTime? @updatedAt
  all_resumes String?   @db.LongText
}
